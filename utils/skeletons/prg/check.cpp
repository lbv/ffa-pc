#include <cassert>
#include <cstdio>

#define MAXT 100



int main()
{
	int T;
	scanf("%d", &T);
	assert(1 <= T && T <= MAXT);

	for (int t = 1; t <= T; ++t) {
		printf("Testing case %d ..\n", t);
		scanf("", );
		assert();
	}

	puts("OK");

	return 0;
}
